#set( $cols = [] )
#set( $vals = [] )
#foreach( $entry in $ctx.args.createUsersInput.keySet() )
  #set( $discard = $cols.add($entry) )
  #set( $discard = $vals.add("'$ctx.args.createUsersInput[$entry]'") )
#end
#set( $valStr = $vals.toString().replace("[","(").replace("]",")") )
#set( $colStr = $cols.toString().replace("[","(").replace("]",")") )
{
  "version": "2018-05-29",
  "statements":   ["INSERT INTO Users $colStr VALUES $valStr", "SELECT * FROM Users WHERE id=$ctx.args.createUsersInput.id"]
}