#set( $cols = [] )
#set( $vals = [] )
#foreach( $entry in $ctx.args.createPostsInput.keySet() )
  #set( $discard = $cols.add($entry) )
  #set( $discard = $vals.add("'$ctx.args.createPostsInput[$entry]'") )
#end
#set( $valStr = $vals.toString().replace("[","(").replace("]",")") )
#set( $colStr = $cols.toString().replace("[","(").replace("]",")") )
{
  "version": "2018-05-29",
  "statements":   ["INSERT INTO Posts $colStr VALUES $valStr", "SELECT * FROM Posts WHERE id=$ctx.args.createPostsInput.id"]
}